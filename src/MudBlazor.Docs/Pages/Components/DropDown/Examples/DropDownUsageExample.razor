@using Microsoft.AspNetCore.Components
@using System.Globalization;
@namespace MudBlazor.Docs.Examples

<MudDropDown T="int" @bind-Value="_value" Items=@Values Label="Select number" HelperText="A long list" UseVirtualization=true Context="item" Dense=true>
    Song number @(item==2?"Two":item.ToString())
</MudDropDown>

<MudNumericField T=int @bind-Value=@_value Label="The value" HelperText="The current value"/>

@code {
    private int _value;
    private int[] Values = Enumerable.Range(0, 1000).ToArray();
}
