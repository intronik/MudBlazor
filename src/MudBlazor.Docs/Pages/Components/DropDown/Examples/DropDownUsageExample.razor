@using Microsoft.AspNetCore.Components
@using System.Globalization;
@namespace MudBlazor.Docs.Examples

<MudDropDown T="int" @bind-Value="_value" Items=@Values Label="Select number" HelperText="A long list" UseVirtualization=true Context="item" FullWidth=true>
    Song number @(item==2?"Two":item.ToString())
</MudDropDown>

@code {
    private int _value;
    private int[] Values = Enumerable.Range(0, 1000).ToArray();
}
